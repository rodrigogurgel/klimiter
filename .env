# Profile padrão do docker-compose: 'standalone' (klimiter + redis).
# Para o cluster, sobrescreva no comando: `COMPOSE_PROFILES=redis-cluster docker compose up`
# (ou use `make up-cluster`). Os dois modos são mutuamente exclusivos.
COMPOSE_PROFILES=standalone

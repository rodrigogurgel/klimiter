klimiter:
  domains:
    - id: default
      descriptors:
        - key: user_id
          rule:
            unit: SECOND
            requests-per-unit: 100
        - key: product_id
          rule:
            unit: MINUTE
            requests-per-unit: 10

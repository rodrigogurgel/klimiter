apiVersion: v1
kind: Service
metadata:
  name: klimiter
  namespace: klimiter
  labels: { app.kubernetes.io/name: klimiter }
spec:
  selector: { app.kubernetes.io/name: klimiter }
  ports:
    - { name: grpc, port: 9090, targetPort: grpc }

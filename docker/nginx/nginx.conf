events {}

http {
  upstream grpc_cluster {
    server app-1:9090;
    server app-2:9090;
    server app-3:9090;
  }

  server {
    listen 9090 http2;

    location / {
      grpc_pass grpc://grpc_cluster;
    }
  }
}